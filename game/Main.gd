extends Node2D

# Declare member variables here. Examples:
# var a = 2
# var b = "text"

# var bluemoon = load("res://bin/gdbluemoon.gdns").new()

# Called when the node enters the scene tree for the first time.
func _ready():
	# print(get_tree().get_root().get_children()[0].get_children()[0].name)
	# var playersNode = get_node("Players")
	# var playerScene = load("res://Player.tscn")
	# var player1 = playerScene.instance()
	# var player2 = playerScene.instance()
	# player1.set_meta("id", 1)
	# playersNode.add_child(player1, true)
	# playersNode.add_child(player2, true)
	# print(playersNode.get_children()[0].get_meta("id"))
	pass

# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
